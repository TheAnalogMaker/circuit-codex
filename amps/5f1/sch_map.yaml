# amps/5f1/sch_map.yaml — schematic <-> netlist reconciliation.
# Data, never a guess in code: every line below is a claim a reviewer can check
# against this sheet and against netlist.cir. See docs/schematic-nets.md.

anchors:
  T2.1: BP1                # OT primary, V2 plate end — primary DCR omitted
  T2.2: BP1                # OT primary, B+ end
  T2.3: "0"                # OT secondary hot — secondary DCR folded into ground
                           # (R13's feedback return); mirrors layout.yaml
                           # net_map anchor SPK: '0'.
